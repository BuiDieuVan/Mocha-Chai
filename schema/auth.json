{
    "required": [
        "email",
        "password"
    ],
    "properties": {
        "email": {
            "type": "string"
        },
        "password": {
            "type": "string"
        },
        "osType": {
            "type": "string",
            "enum": [
                "ios",
                "android",
                "web"
            ]
        }
    },
    "errorMessage": {
        "type": "should be an object",
        "required": {
            "password": "StartTime not blank",
            "email": "EndTime not blank"
        }
    }
}